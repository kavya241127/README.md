# Auto detect text files and perform LF normalization
* text=auto
# Normalize all text files and ensure LF line endings
* text=auto

# Ensure consistent line endings for Python and related files
*.py    text eol=lf
*.sh    text eol=lf

# Treat batch scripts as binary to preserve CRLF line endings
*.bat   text eol=crlf

# Treat binary files as-is (no normalization)
*.png   binary
*.jpg   binary
*.gif   binary
*.pdf   binary

# Handle documentation and log files
*.md    text eol=lf
*.txt   text eol=lf
*.log   text eol=lf
